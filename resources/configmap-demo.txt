# uid-dp-velocity-engine-control-plane-frontend @ demo
# Sourced from src/config/appConfig.js on this branch.
# Kubernetes ConfigMap-data style: KEY=value, one per line.
# Values shown are this branch's coded defaults (used when the key is
# unset). <SET_IN_ENV> marks a key with no safe default here (never a real
# value copied from any local .env/config).
#
# Only keys actually resolved from the environment are listed below — via
# getEnvVar() in appConfig.js, which checks window._env_[key] (runtime
# override, see public/env-config.js in Docker deployments) then
# import.meta.env[key] (Vite build-time env var) before falling back to the
# default shown. Everything else appConfig.js exports (API_BASE, WS_BASE,
# WS_RECONNECT_*, LIVE_STORE_MAX_ROWS, LIVE_POLL_INTERVAL,
# HISTORICAL_MAX_DAYS, DEFAULT_SOURCE_TOPIC, DEFAULT_PENALTY_TTL_SEC,
# DEFAULT_WINDOW_SIZE_SEC, DEFAULT_SLIDE_SEC, MAX_AGGREGATIONS) is a
# hardcoded build-time constant with no environment override — there is no
# env var name for these, so they have no entry in a ConfigMap. (No
# ME_ENDPOINT or AUTH_DEBUG_* constants exist on this branch at all — this
# branch has no /me endpoint and no debug-identity system; see CLAUDE.md.)
#
# REACT_APP_CLIENT_SECRET: appConfig.js on this branch ships a real hardcoded
# fallback value for this key (see the file's own "SECURITY NOTE" comment —
# a known, already-flagged issue on this branch, not fixed here since fixing
# it is a WSO2 client behavior change out of scope for this file). That real
# value is deliberately NOT reproduced here — use <SET_IN_ENV>, same as any
# other credential.

REACT_APP_WSO2_AUTHORITY=https://sso.uidai.net.in/oauth2  # WSO2 Identity Server authority (base URL)
REACT_APP_CLIENT_ID=9HGuTetQjRjxkx1vHmoP1v0fXm8a  # WSO2 OAuth client id
REACT_APP_CLIENT_SECRET=<SET_IN_ENV>  # WSO2 OAuth client secret — sensitive, see note above
REACT_APP_REDIRECT_URI=http://localhost:3000/callback  # OAuth redirect_uri after WSO2 login
REACT_APP_POST_LOGOUT_REDIRECT_URI=http://localhost:3000/  # redirect target after WSO2 logout
REACT_APP_SILENT_REDIRECT_URI=https://ilabel.uidai.net.in/silent-renew  # redirect_uri used for silent token renewal
